// import { test, expect } from '@playwright/test';
// test('dropdown select', async ({ page }) => {
//   await page.goto('https://www.w3schools.com/tags/tryit.asp?filename=tryhtml_select');
//   const frame = page.frameLocator('#iframeResult');

//   await frame.locator('select').selectOption('volvo');
//   await expect(frame.locator('select')).toHaveValue('volvo');
// });

// Concepts

// selectOption() → chooses value in a <select> dropdown.

// Assertion: toHaveValue().